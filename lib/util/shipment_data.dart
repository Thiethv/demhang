class ShipmentData {
  final String refNo;
  final int totalCtn;
  final int diff;
  final int scanned;
  final int counted;

  ShipmentData(this.refNo, this.totalCtn, this.diff, this.scanned, this.counted);
}